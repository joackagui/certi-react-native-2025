export type Hobby = {
  id: string;
  title: string;
  icon: string;
};

export type Skill = {
  id: string;
  title: string;
};

export type Task = {
  id: string;
  title: string;
  description: string;
  due: string;
  completed: boolean;
};
